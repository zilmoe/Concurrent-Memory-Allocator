// insert an address into the bloom filter
void insert(int);

// remove an address from the bloom filter
void remove_exis(int);

// check if the address is in the bloom filter
bool check_existance(int);
